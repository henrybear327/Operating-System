#include <stdio.h>
#include <stdlib.h>

int main() {

  int *array = malloc(sizeof(int) * 10485760);

  int n;

  scanf("%d", &n);

  for (int i = 0; i < 10485760; i++) {
    array[i] = i + 1;
  }

  scanf("%d", &n);

  return 0;
}
