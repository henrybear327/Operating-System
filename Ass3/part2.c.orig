#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {

  int size = 0;

  while (1) {
    int *array = malloc(1024);
    size++;
    if (array == NULL) {
      break;
    }
  }

  printf("Now alloc %d\n", size / 1024);

  return 0;
}
